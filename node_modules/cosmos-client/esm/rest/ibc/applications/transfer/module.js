import { QueryApi } from '../../../../openapi/api';
export function denomTraces(sdk, paginationKey, paginationOffset, paginationLimit, paginationCountTotal) {
    return new QueryApi(undefined, sdk.url).denomTraces(paginationKey, paginationOffset?.toString(), paginationLimit?.toString(), paginationCountTotal);
}
export function denomTrace(sdk, hash) {
    return new QueryApi(undefined, sdk.url).denomTrace(hash);
}
export function params(sdk) {
    return new QueryApi(undefined, sdk.url).iBCTransferParams();
}
