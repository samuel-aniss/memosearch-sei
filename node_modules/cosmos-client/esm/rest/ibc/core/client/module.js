import { QueryApi } from '../../../../openapi/api';
export function clientStates(sdk, paginationKey, paginationOffset, paginationLimit, paginationCountTotal) {
    return new QueryApi(undefined, sdk.url).clientStates(paginationKey, paginationOffset?.toString(), paginationLimit?.toString(), paginationCountTotal);
}
export function clientState(sdk, clientID) {
    return new QueryApi(undefined, sdk.url).clientState(clientID);
}
export function consensusStates(sdk, clientID) {
    return new QueryApi(undefined, sdk.url).consensusStates(clientID);
}
export function consensusState(sdk, clientID, revisionNumber, revisionHeight, latestHeight) {
    return new QueryApi(undefined, sdk.url).consensusState(clientID, revisionNumber.toString(), revisionHeight.toString(), latestHeight);
}
