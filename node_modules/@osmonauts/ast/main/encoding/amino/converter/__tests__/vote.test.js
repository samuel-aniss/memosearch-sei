"use strict";

var _index = require("../index");

var _protoParser = require("@osmonauts/proto-parser");

var _case = require("case");

var _testUtils = require("../../../../../test-utils/");

var store = new _protoParser.ProtoStore([__dirname + '/../../../../../../../__fixtures__/chain1']);
store.traverseAll();
describe('cosmos/gov/v1beta1/tx', function () {
  var _prepareContext = (0, _testUtils.prepareContext)(store, 'cosmos/gov/v1beta1/tx.proto'),
      context = _prepareContext.context,
      root = _prepareContext.root,
      protos = _prepareContext.protos;

  it('AminoConverter', function () {
    context.options.aminoEncoding.casingFn = _case.snake;
    (0, _testUtils.expectCode)((0, _index.createAminoConverter)({
      context: context,
      root: root,
      name: 'AminoConverter',
      protos: protos
    }));
  });
});