import { cosmos } from "../../proto"; // Supported transaction interfaces - types/proto/cosmos/vesting/v1beta1/tx.rpc.msg.d.ts
//---------------------------------------------
// COSMOS::VESTING MODULE - TRANSACTION HELPERS
// --------------------------------------------
//MsgCreateVestingAccount

export async function execute_cosmos_vesting_create_vesting_account(fromAddress, toAddress, amount, endTime, delayed) {
  let msg = cosmos.vesting.v1beta1.MsgCreateVestingAccount.fromPartial({
    fromAddress,
    toAddress,
    amount: amount,
    endTime,
    delayed
  });
  return msg;
}