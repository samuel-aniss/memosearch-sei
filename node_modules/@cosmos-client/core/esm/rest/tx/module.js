import { ServiceApi, BroadcastTxRequestModeEnum as BroadcastTxMode, } from '../../openapi/api';
export { BroadcastTxMode };
export function broadcastTx(sdk, body) {
    return new ServiceApi(undefined, sdk.url).broadcastTx(body);
}
export function getTx(sdk, hash) {
    return new ServiceApi(undefined, sdk.url).getTx(hash);
}
export function getTxsEvent(sdk, events, paginationKey, paginationOffset, paginationLimit, paginationCountTotal, paginationReverse, orderBy, page, limit) {
    return new ServiceApi(undefined, sdk.url).getTxsEvent(events, paginationKey, paginationOffset?.toString(), paginationLimit?.toString(), paginationCountTotal, paginationReverse, orderBy, page, limit);
}
export function simulate(sdk, body) {
    return new ServiceApi(undefined, sdk.url).simulate(body);
}
